import styled from 'styled-components'

const Search = styled.div `
    @extend %center;
    position:relative;
    display: flex;
    width: 60%;
    border-radius: 5px;
    padding: 0 10px;
`;

export default Search