import styled from 'styled-components'

const Input = styled.input.attrs({
    type: "text"
})`
    position: relative;
    margin: auto;
    width: 100%;
    max-width: 280px;
`;

export default Input